<div class="bg-primary text-primary-content px-6 py-4 flex items-center justify-center relative">
  <div>
    <h1 class="text-3xl font-bold text-center">{{ title }}</h1>
    <p class="opacity-80 text-center text-sm mt-1">{{ subtitle }}</p>
  </div>
  <button id="langToggle" class="btn btn-ghost btn-sm absolute right-4 text-primary-content">
    EN
  </button>
</div>